# MongoDB connection (local)
MONGODB_URI=mongodb://localhost:27017
# Database name
MONGODB_DB=lostandfound

# Server port
PORT=5000

# JWT secret (change in production)
JWT_SECRET=your_super_secret_key_change_this_later
